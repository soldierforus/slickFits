## database calls will go in this file

type Dog {
    name: String!
}


type Mutation {
    createDog(name:String!) : Dog
}

##adding ! after array item means we cannot return null for any item
type Query {
    dogs: [Dog!]
}